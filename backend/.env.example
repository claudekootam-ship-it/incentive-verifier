# Local dev only — copy to .env (gitignored) and fill in. In production these
# live in Google Secret Manager (see app/config.py), never in a deployed .env.

GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
PARALLEL_API_KEY=
GOOGLE_MAPS_API_KEY=
