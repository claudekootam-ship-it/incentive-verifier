# Secrets — never commit
.env
.env.*
!.env.example
*.key.json
service-account*.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
dist/
build/

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/
npm-debug.log*

# Firebase CLI's local deploy cache — machine-specific, not config
.firebase/

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
Thumbs.db

# Generated intermediate for the sample budget (the PDF itself is committed)
samples/sample_budget.html
